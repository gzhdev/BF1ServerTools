<UserControl
    x:Class="BF1ServerTools.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="clr-namespace:BF1ServerTools.Themes.Controls"
    d:DesignHeight="720"
    d:DesignWidth="1280"
    DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <!--  ////////////////////  -->
        <RichTextBox
            Grid.Column="0"
            Margin="5"
            Style="{StaticResource RichTextBoxStyle_Normal}">
            <FlowDocument>
                <Paragraph Style="{StaticResource ParagraphStyle_H18}">
                    使用说明
                </Paragraph>

                <Paragraph Style="{StaticResource ParagraphStyle_H14}">
                    ● 建议最低分辨率1280*720，缩放比例100%
                </Paragraph>
                <Paragraph Style="{StaticResource ParagraphStyle_H14}">
                    ● 如果网络环境不佳，可以尝试使用HTTP代理
                </Paragraph>
            </FlowDocument>
        </RichTextBox>
        <!--  ////////////////////  -->
        <!--  ////////////////////  -->
    </Grid>

</UserControl>
